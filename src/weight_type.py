from decimal import Decimal
# This variable determines the type of the weights of the algorithm

WEIGHT_TYPE = float

types = {
        "decimal": Decimal,
        "float": float,
        "int": int
    }